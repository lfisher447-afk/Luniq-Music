name: Build Windows EXE

on:
  push:
    tags:
      - 'v*' # Triggers on version tags (e.g., v1.0.6)
  workflow_dispatch: # Allows manual button-click testing

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write # CRITICAL: Gives the token authority to create/edit releases

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        # Runs npm ci, triggering your verification scripts and native rebuilds
        run: npm ci

      - name: Build and Package Executable
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Build Artifacts (For Manual Runs)
        # This guarantees you get your EXE even when testing manually without a tag!
        uses: actions/upload-artifact@v4
        with:
          name: Luniq-Music-Windows-EXE
          path: |
            dist/*.exe
            dist/*.blockmap
            release/*.exe
            release/*.blockmap
            dist-electron/*.exe
            dist-electron/*.blockmap
          if-no-files-found: warn

      - name: Deploy to GitHub Release (For Version Tags Only)
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.exe
            dist/*.blockmap
            release/*.exe
            release/*.blockmap
            dist-electron/*.exe
            dist-electron/*.blockmap
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
